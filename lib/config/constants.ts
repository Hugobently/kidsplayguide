/**
 * App-wide constants and configuration values.
 */

/**
 * Number of games to display per page in listings.
 */
export const GAMES_PER_PAGE = 12;

/**
 * Maximum allowed limit for game queries (prevents abuse).
 */
export const MAX_GAMES_LIMIT = 50;
